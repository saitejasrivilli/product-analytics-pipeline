services:
  - type: web
    name: product-analytics-api
    env: python
    pythonVersion: 3.11
    buildCommand: pip install fastapi uvicorn duckdb motherduck pandas
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MOTHERDUCK_TOKEN
        sync: false
